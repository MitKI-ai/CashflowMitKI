version: "3.9"
services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8080}:8000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/subscriptions.db
    restart: unless-stopped
